/* lsd.h

   A header for the main file (lsd.cpp).
   Used to wrap lsd in cython.

   Written by Jesse Bloom
*/

using namespace std;

// the main program for lsd, defined in lsd.cpp
// will return 0 if and only if program executes successfully
int lsd(int argc, char** argv);
